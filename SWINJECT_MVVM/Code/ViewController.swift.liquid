{% include 'header' %}

import UIKit
import RxSwift
import RxCocoa

class {{ module_info.name }}ViewController: ParentViewController {

    let viewModel: {{ module_info.name }}ViewModel

    init(viewModel: {{ module_info.name }}ViewModel) {
        self.viewModel = viewModel
        super.init()
    }

    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
}

extension {{ module_info.name }}ViewController {
    override func viewDidLoad() {
        super.viewDidLoad()

    }
}

extension {{ module_info.name }}ViewController: Routable {

}