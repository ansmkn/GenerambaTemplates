{% include 'header' %}

import XCTest
import RxTests
import RxBlocking
import Swinject

class {{ module_info.name }}ViewControllerTests: XCTestCase {
    
    let assembler: Assembler = {
        let assembler = Assembler()
        
        assembler.apply(assembly: {{ module_info.name }}Assembly())
        return assembler
    }()
    
    var viewController: {{ module_info.name }}ViewController!
    
    override func setUp() {
        super.setUp()
        self.viewController = assembler.resolver.resolve({{ module_info.name }}ViewController.self)!
    }
    
    override func tearDown() {
        self.viewController = nil
        super.tearDown()
    }
    
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }
    
    func testPerformanceExample() {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }
    
}
