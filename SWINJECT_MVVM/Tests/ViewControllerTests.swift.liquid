{% include 'header' %}

import XCTest
import RxTest
import RxBlocking
import Swinject

// swiftlint:disable force_unwrapping
class {{ module_info.name }}ViewControllerTests: XCTestCase {

    let assemblies = Assemblies()

    var viewController: {{ module_info.name }}ViewController!

    override func setUp() {
        super.setUp()
        let resolver = assemblies.assembler.resolver
        guard let vc = resolver.resolve({{ module_info.name }}ViewController.self) else {
            print("Factory broken")
            return
        }
        self.viewController = vc
    }

    override func tearDown() {
        self.viewController = nil
        super.tearDown()
    }

    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }

    func testPerformanceExample() {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }

}
