{% include 'header' %}

import XCTest
import RxTest
import RxBlocking
import Swinject

class {{ module_info.name }}ViewModelTests: XCTestCase {
    
    let assemblies = Assemblies()

    var viewModel: {{ module_info.name }}ViewModel!

    override func setUp() {
        super.setUp()
        let resolver = assemblies.assembler.resolver
        guard let vm = resolver.resolve({{ module_info.name }}ViewModel.self) else {
            fatalError("Factory broken")
        }
        self.viewModel = vm
    }

    override func tearDown() {
        self.viewModel = nil
        super.tearDown()
    }

    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }

    func testPerformanceExample() {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }

}
